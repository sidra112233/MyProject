<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Inbound Calls - Filters</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #f8fafc;
      margin: 0;
      padding: 0;
      color: #0f172a;
    }

    .header {
      background-color: #1e293b;
      color: #fff;
      text-align: center;
      padding: 30px 20px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
    }

    .header h1 {
      margin: 0;
      font-size: 32px;
      font-weight: 700;
    }

    .filter-section {
      background-color: #ffffff;
      max-width: 1100px;
      margin: 40px auto;
      padding: 40px 30px;
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.06);
    }

    .filters {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 25px;
    }

    .filter-item label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
      font-size: 15px;
    }

    .filter-item select,
    .filter-item input {
      width: 100%;
      padding: 10px 14px;
      border: 1px solid #cbd5e1;
      border-radius: 10px;
      background-color: #f1f5f9;
      font-size: 15px;
      color: #0f172a;
      transition: 0.3s border-color;
    }

    .filter-item select:focus,
    .filter-item input:focus {
      border-color: #14b8a6;
      outline: none;
    }

    .submit-btn {
      grid-column: span 2;
      display: flex;
      justify-content: flex-end;
      margin-top: 20px;
    }

    .submit-btn button {
      background-color: #14b8a6;
      color: #fff;
      font-size: 16px;
      padding: 12px 32px;
      font-weight: 600;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .submit-btn button:hover {
      background-color: #0d9488;
    }

    @media (max-width: 768px) {
      .submit-btn {
        grid-column: span 1;
        justify-content: center;
      }
    }
  </style>
</head>
<body>

  <div class="header">
    <h1><i class="fas fa-headset"></i> Inbound Call Filters</h1>
  </div>

  <div class="filter-section">
    <form class="filters" method="GET" action="/inbound/calls">
      <div class="filter-item">
        <label for="customer"><i class="fas fa-user"></i> Customer</label>
        <select id="customer" name="customer">
          <option value="">All Customers</option>
          <!-- You can inject customer options here dynamically -->
          <option value="John Doe">John Doe</option>
          <option value="Jane Smith">Jane Smith</option>
        </select>
      </div>

      <div class="filter-item">
        <label for="duration"><i class="fas fa-clock"></i> Duration (sec)</label>
        <input type="number" id="duration" name="duration" placeholder="e.g. 60">
      </div>

      <div class="filter-item">
        <label for="date"><i class="fas fa-calendar"></i> Date</label>
        <input type="date" id="date" name="date">
      </div>

      <div class="submit-btn">
        <button type="submit"><i class="fas fa-filter"></i> Apply Filters</button>
      </div>
    </form>
  </div>

</body>
</html>
